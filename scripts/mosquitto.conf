# Local Stikka-NG MQTT broker for browser + mock bridge testing

# Disable persistence for fast disposable local runs
persistence false
allow_anonymous true

# Raw MQTT for bridge script
listener 1883 127.0.0.1
protocol mqtt

# MQTT over WebSocket for frontend (browser)
listener 9001 127.0.0.1
protocol websockets
